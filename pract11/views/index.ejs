<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>QR-генератор</title>
</head>
<body>
    <h1>Генератор QR-кода</h1>
    
    <form action="/generate" method="POST">
        <input type="text" name="text" placeholder="Введите текст или URL" required>
        <br><br>
        
        <label>Цвет QR-кода:</label>
        <input type="color" name="darkColor" value="#000000">
        <br><br>
        
        <label>Цвет фона:</label>
        <input type="color" name="lightColor" value="#ffffff">
        <br><br>
        
        <button type="submit">Сгенерировать</button>
    </form>

    <% if (qrcode) { %>
        <h2>QR-код:</h2>
        <img src="<%= qrcode %>" alt="QR Code">
    <% } %>
</body>
</html>